# Optional: Gemini API Key for AI Copilot (Text-to-Insights and Automated Executive Briefings)
# The dashboard has an intelligent built-in fallback mode and runs 100% offline if this is omitted.
GEMINI_API_KEY=your_gemini_api_key_here
